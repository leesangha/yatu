import { TreemapChart } from "@toast-ui/react-chart";
import React from "react";

export default function TreeMapChart({ data, options }: any) {
  return <TreemapChart data={data} options={options} />;
}
